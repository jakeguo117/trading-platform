---
title: "GLD 最终需求、交易规则与偏好"
document_type: "project-handoff"
source_project: "/Users/jake/Desktop/GLD_MD_Project_2026-08-08"
captured_on: "2026-08-27"
---

# GLD 最终需求、交易规则与偏好

这份文件的主体只保留两类内容，并在末尾附研究文件夹入口：

1. 产品最终必须解决的四个问题；
2. 旧项目已经确认或明确表达过的交易规则和偏好。

所有实验、比较、候选参数、失败结论和证据路径均放在 [研究成果](研究成果/README.md) 文件夹。

## 1. 最终产品需求

产品每次形成可执行结果时，必须同时回答：

1. **什么时候买？**
2. **买什么？**
3. **买多少？**
4. **什么时候卖？**

四个问题必须基于同一份封存事实、同一版本 Policy 和同一版本策略规则。只回答其中一部分，不构成完整产品结果。

### 1.1 什么时候买

产品需要给出明确的入场状态：

- `可以买`：方向、时机、数据、仓位和风险条件全部通过；
- `今天不买`：完整计算后确认没有合格机会；
- `无法判断`：数据、规则、Policy 或证据不足。

历史规则和偏好：

- 只在允许做多的慢趋势中寻找机会；趋势向下或不清楚时保持现金。
- 慢趋势决定是否有资格做多，快信号决定当天是否行动。
- 不追单日孤立突破或孤立盘前大涨；Breakout 需要后续保持、结构延续和确认。
- 价格结构、均线位置/方向、突破/回撤和量能决定方向；IV、Greeks 和流动性只能否决工具，不能创造看多方向。
- 必须先检查已有仓位、open orders、部分成交和人工修改，再考虑新开仓。
- Breakout 的生产公式尚未验证完成；在 Gate A 通过前，不能把历史候选当成已确认买点。

一个有效买点至少要绑定：交易日、decision time、signal/rule version、输入 hash、有效期、失效条件和不买原因。

### 1.2 买什么

产品需要从已经独立验证合格的结构中，选择一个完整、可执行的合约组，而不是只给模糊方向。

输出至少包括：

- 策略/结构名称；
- 到期日；
- 每一腿的 Call/Put、strike、买卖方向和整数数量；
- 完整组合关系；
- executable bid/ask、费用和失效条件；
- 为什么选择它，以及为什么没有选择其他结构。

历史规则和偏好：

- 多腿交易必须整组成立；缺一腿、身份不唯一或报价不完整时不能留下部分指令。
- `MOMENTUM_CALL`、`BULL_CALL_SPREAD`、`PUT_CREDIT_SPREAD` 可以共享原始事实，但各自的假设、参数、成本、walk-forward、blind 和结论必须独立。
- Long Call 的历史偏好是保留不封顶上行。
- BCS 只有在明确接受封顶上行，以换取较低成本或较浅下行时才可考虑。
- 不允许未经验证的市场状态自动在 Call 与 BCS 之间切换，也不允许一个 carrier 失败后 silent fallback 到另一个。
- 当前研究尚未形成获准的最终 carrier selection rule；不能把小样本 Call/BCS 比较直接变成实时选择。

### 1.3 买多少

产品必须输出整数个完整合约组；数量可以为零，但不能靠猜测生成。

计算顺序：

1. 重建现有持仓和 pending orders 的完整暴露；
2. 计算 proposed group 的最大损失和计划退出损失；
3. 计算交易后 GLD Delta、现金、drawdown capacity 和流动性；
4. 分别得到各项最大允许数量；
5. 取其中最小的非负整数。

历史已确认风险规则：

| 项目 | 旧 Policy 规则 |
|---|---|
| 账户范围 | 只有 IBKR 进入 sizing；其他账户只报告，不影响计算 |
| 已实现利润 | 50% 可再投入，50% 隔离 |
| 未实现利润 | 不扩大 eligible bankroll |
| Kelly | Robust lower-confidence-bound Kelly 的 50%，禁止在线再训练 |
| 最大 GLD Delta | Eligible bankroll 的 150% |
| 交易后 settled cash | 至少为 NLV 的 10% |
| Drawdown 0–5% | Capacity 100% |
| Drawdown 5–10% | Capacity 80% |
| Drawdown 10–15% | Capacity 40% |
| Drawdown 15–20% | 停止新开 |
| Drawdown 20–30% | 退出 managed groups |
| Drawdown ≥30% | Sticky disaster lock |

其他偏好：

- 首次开仓应小型试探，不接近 all-in。
- 只给已经按预期表现、趋势继续确认的赢家加仓。
- 不向下摊平，不因亏损扩大仓位。
- Pending 暴露在 fresh terminal order state 前不能从风险计算中消失。
- 缺 max-loss、planned-loss、Delta、cash、drawdown、费用或流动性事实时，数量不得生成。

这些数字属于旧项目 Policy；迁入新平台前必须重新确认并形成唯一 hash-bound Policy。

### 1.4 什么时候卖

产品必须先管理已有仓位，再考虑新开仓。每个持仓组都需要明确：

- 正常持有条件；
- signal invalidation；
- 计划止损和风险硬边界；
- 盈利管理；
- expiry safety；
- maximum hold；
- 完整组合退出动作。

历史规则和偏好：

- 快信号失败或动量消退时，Momentum Call 快速退出。
- 慢趋势失效、产品风险、盈利复核或到期复核可以触发 BCS 减仓或退出。
- PCS 接近或失守技术边界/short-put 安全区时主动防守，不等待最大亏损。
- 多腿组合完整退出，不能只处理一条腿。
- 所有期权必须在到期前复核，不被动持有至到期。
- “滚动/置换”不是自动续命，而是完整退出旧组，再按当天条件重新批准新组。
- 下跌跳空触及仓位风险线或慢趋势边界时，优先执行预设退出，不赌反弹。
- 部分成交、未知状态或人工修改未完成重建时，停止新的开仓建议。

历史五交易日 maximum hold、退出优先级和新 15-session 方案都仍是候选或未运行研究；最终卖出规则尚需从研究成果中重新选择、验证并绑定。

## 2. 完整结果的验收标准

### `ACTION_PLAN`

必须同时包含：

- 什么时候买或卖；
- 买/卖什么完整合约组；
- 整数数量；
- 入场/退出、有效期和失效条件；
- 对应事实、Policy、规则和 decision ID。

### `NO_ACTION`

表示数据和规则都完整，计算后确认今天不需要开仓、调整或退出。它不能用来表示“还在等数据”。

### `NO_DECISION`

表示至少一个关键事实、规则、Policy 或风险计算不足。必须清空交易指令，并说明：

- 缺什么；
- 影响哪个问题；
- 为什么不能判断；
- 满足什么条件后可以重新计算。

只要四个问题中有一个无法安全回答，就不能输出伪精确的 `ACTION_PLAN`。

## 3. 共同交易规则和偏好

- 第一版历史范围只做 GLD。
- 只做顺势做多；趋势弱或不清楚时可以全现金。
- 宏观叙事只作背景，不能推翻价格趋势或直接制造交易方向。
- 硬 Gate 先于候选排序；边缘机会宁可不做。
- 每笔开仓前必须写清入场理由、计划止损、盈利管理、失效条件、到期复核和退出路径。
- 调整必须产生足以覆盖 spread、fees、slippage、税（如适用）和新增风险的预期净改善。
- 不设账户总止盈线；各模块管理盈利，账户层负责保护本金和收缩风险。
- 回撤恶化只能保持或缩小风险容量；恢复不能靠未实现浮盈或追加资金伪装。
- 人工执行后必须通过下一次只读快照核验，不能因用户说“已执行”就假设成交。
- 系统只形成决策和人工执行说明，不创建、修改、取消或提交 broker 订单。
- 同一输入、Policy 和规则版本必须得到相同结果；LLM 只解释已封存结论，不能计算或修改交易答案。
- 缺身份、来源时间、完整性、新鲜度、报价状态、Policy 或共享风险事实时，必须 `NO_DECISION`。

## 4. 模块偏好摘要

### Momentum Call

- 用于已确认上行趋势后的短期加速。
- 首次小型试探，只加赢家。
- 快信号失败、动量消退或止损触发时快速退出。
- 不是 LEAP 替代，也不是临近到期赌博。

### Bull Call Spread

- 用于确认后的中期稳定上行。
- Long Call 跟随趋势，Short Call 对应可解释目标。
- 净支出、计划/极端风险和剩余上行都必须合格。
- 开、持、调整和退出始终整组处理。

### Put Credit Spread

- 只在上行趋势完整且偏强盘整时考虑。
- Short Put 在技术支撑下方留安全距离，Long Put 封顶尾部风险。
- 权利金不足以补偿风险和摩擦时不做。
- 不向下滚动硬扛；完整退出后才可重新批准。

这些是历史定性偏好，不是已经完成量化验证的实时模块规则。

## 5. 数据和决策时点偏好

- Post-close：形成非执行预案和次日观察条件。
- Premarket：只更新方向和风险，不能形成最终期权执行卡。
- Post-open：全部目标腿取得当日 active、完整、fresh 双边报价后，才可能给出 `ACTION_PLAN`。
- 旧 F2 合同曾要求目标腿报价不超过 120 秒，动态事实组跨度不超过 300 秒。
- IBKR 是旧 v1 daily read-only facts 的优先来源；缺失时不能 silent fallback 到未经确认的实时来源。
- 空仓、无订单或无成交也必须由权威全集明确证明，不能由空数组或模型推断。

这些时点和 freshness 数值属于旧合同，进入新平台前需要重新确认。

## 6. 研究成果

研究结果不再放在主 handoff：

- [研究成果总览](研究成果/README.md)
- [Breakout 与 Call/BCS 研究成果](研究成果/01-Breakout与Call-BCS研究成果.md)
- [其他策略结构研究成果](研究成果/02-其他策略结构研究成果.md)
- [风险与数据研究成果](研究成果/03-风险与数据研究成果.md)
